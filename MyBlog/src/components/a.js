import React, { PureComponent } from 'react';

export default class A extends PureComponent {
  render() {
    const layout = (
      <div>111111111</div>
    );
    return (
      <div>{layout}</div>
    );
  }
}
